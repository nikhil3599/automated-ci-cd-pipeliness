{
  "name": "automated-ci-pipeline-jenkins",
  "version": "1.0.0",
  "description": "A simple Node.js web app for Jenkins CI pipeline demo",
  "main": "src/app.js",
  "scripts": {
    "test": "jest",
    "build": "zip -r build/artifact.zip src/"
  },
  "devDependencies": {
    "eslint": "^8.57.1",
    "jest": "^29.7.0"
  },
  "dependencies": {
    "express": "^4.21.2",
    "supertest": "^7.1.4"
  }
}
